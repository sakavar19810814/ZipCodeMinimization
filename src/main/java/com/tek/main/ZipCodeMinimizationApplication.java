package com.tek.main;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
//This class not required to run the functionality and created as a part spring boot app.
@SpringBootApplication
public class ZipCodeMinimizationApplication {
	
	public static void main(String[] args) {
		SpringApplication.run(ZipCodeMinimizationApplication.class, args);
	}

}
